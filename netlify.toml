[build]
  command = "npm run build"
  publish = "dist/krh-auto/browser"

[dev]
  command = "npm start"
  port = 4562
  targetPort = 4562

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"